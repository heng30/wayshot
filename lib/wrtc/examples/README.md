### How to test?
- run: `RUST_LOG=debug cargo run --example whep_server_demo`

- open web broswer and visit `http://localhost:9090`

### Troubleshot
    - `Firefox`: Only can play video stream through `http://localhost:9090`. Due to its' security rules.
