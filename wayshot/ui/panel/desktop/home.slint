import { Theme, Store, TabIndex, Util } from "../def.slint";
import { Blanket } from "../../base/widgets.slint";
import { MainPanel } from "main.slint";
import { PlayerPanel } from "player.slint";
import { HistoryPanel } from "history.slint";
import { ShareScreenPanel } from "share-screen.slint";
import { VideoEditor } from "video-editor/video-editor.slint";
import { Logic } from "../../logic.slint";

import {
    ImageAnimationDialog,
} from "video-editor/tools/img-animation/panel.slint";

import {
    BackgroundAnimationDialog,
} from "video-editor/tools/bg-animation/panel.slint";

import {
    SubtitleRemoverDialog,
    SubtitleRemoverSettingDialog,
} from "video-editor/tools/subtitle-remover.slint";

import {
    Terminal,
} from "video-editor/right-panel/terminal.slint";

import {
    SmartMixPanel,
    SmartMixSettingDialog,
} from "video-editor/tools/smart-mix.slint";

import {
    MusicGenDialog,
    MusicGenSettingDialog,
} from "video-editor/tools/musicgen.slint";

import {
    GlobalFilterDialog,
} from "video-editor/tools/global-filter/panel.slint";

import {
    PageFlip,
} from "./video-editor/right-panel/filter/video/page-flip.slint";

export component Home inherits VerticalLayout {
    // Rectangle {
    //     Rectangle {
    //         width: 600px;
    //         PageFlip { }
    //     }
    // }

    // Rectangle {
    //     SmartMixPanel { }
    // //
    // //     SmartMixSettingDialog { }
    // }

    //
    // Rectangle {
    //     BackgroundAnimationDialog {
    //     }
    // }

    // Rectangle {
    //     GlobalFilterDialog {
    //     }
    // }

    // Rectangle {
    //     ImageAnimationDialog {
    //     }
    // }
    //
    if Store.current-tab-index == TabIndex.Home: MainPanel {
        height: parent.height;
    }

    if Store.current-tab-index == TabIndex.History: HistoryPanel {
        height: parent.height;
    }

    if Store.current-tab-index == TabIndex.Player: PlayerPanel {
        height: parent.height;
    }

    if Store.current-tab-index == TabIndex.ShareScreen: ShareScreenPanel {
        width: parent.width;
        height: parent.height;
    }

    if Store.current-tab-index == TabIndex.VideoEditor: VideoEditor {
        height: parent.height;
    }
}
