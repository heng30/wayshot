# "不再提示"勾选即生效，组件不持久化

`UpdateDialog` 中"不再提示"（`dont-ask-again`）只要被勾选即视为用户选择，无论之后点击确认、取消还是关闭按钮都算数；组件只暴露状态，不负责持久化，由调用方在回调中读取并自行保存。遵循主流软件惯例（VS Code、JetBrains 系），并保持 base 组件不碰存储的边界。

_考虑过的方案_：仅确认才算数（被拒，语义复杂且反惯例）；分开记录"不再提示并更新/不更新"（被拒，过度设计）。
